# 前56节阶段试卷 · 批改分析与综合评价

**本次得分：41 / 100。基础部分：40 / 80；综合拔高：1 / 20。**

作答人：崔浩。作答及批改日期：2026-09-17。
依据：[本次原答卷](我的答卷.py)、[试卷要求](试卷.md)、[参考答案与分步评分](参考答案与评分.md)。原答卷保留原样。

## 一、本次成绩说明

| 部分 | 得分 | 满分 | 本次表现 |
|---|---:|---:|---|
| 单项选择 S01—S10 | 16 | 20 | 多数基础规则能识别 |
| 填空 F01—F10 | 10 | 20 | 切片、拆包、字典方法及变量使用不稳定 |
| 判断 J01—J10 | 6 | 10 | 对象是否改变、赋值是否复制容易混淆 |
| 代码找错 D1 | 4 | 15 | 一题修复正确，三题均未填写问题说明 |
| 成绩处理 D2 | 4 | 15 | 有循环、累加、筛选思路，完整运行与输出不足 |
| 订单汇总 D3 | 1 | 20 | 完成分号拆分，其余步骤未完成 |
| **合计** | **41** | **100** | **当前优先补基础编程与调试** |

- 客观题共 **32/50**；编程题共 **9/50**。两者的差距说明需要增加独立书写和运行检查。
- 基础部分得分率为 **50%**，尚未达到本资料原先建议的约 **64/80** 推进参考值。这只是学习安排参考，不是学校正式及格线。
- 答卷记录开始 11:47、结束 12:50，跨度 63 分钟；扣除自述约 10 分钟休息，约 53 分钟。本人填写“大概50分钟”，因此记为约 50—53 分钟，非精确计时。
- 是否查看资料、答案或使用 AI 帮助未记录，不自动认定为完全独立作答。
- 按原评分表逐项给分。语法错误导致整卷无法运行，不代表其他小问全部零分；能确认的正确步骤保留得分。
- 按原卷要求核对输出格式：D2.1 使用英文冒号，扣格式项 1 分；这一扣分不代表循环或拆包概念错误。

## 二、客观题逐题批改

### 单项选择：16/20

| 题号 | 你的答案 | 正确答案 | 得分 | 说明 |
|---|---|---|---:|---|
| S01 | D | B | 0/2 | `input()` 得到字符串，`"18" + "2"` 输出 `182`，不会报类型错误。 |
| S02 | C | C | 2/2 | 正确。 |
| S03 | A | A | 2/2 | 正确。 |
| S04 | A | D | 0/2 | `2` 被 `continue` 跳过，遇到 `4` 时 `break`，只输出 `1、3`。 |
| S05 | B | B | 2/2 | 正确。 |
| S06 | C | C | 2/2 | 正确。 |
| S07 | A | A | 2/2 | 正确，知道连续分隔符会留下空字符串。 |
| S08 | D | D | 2/2 | 正确。 |
| S09 | B | B | 2/2 | 正确。 |
| S10 | C | C | 2/2 | 正确，能识别字典新增与更新。 |

### 填空：10/20

| 题号 | 你的答案 | 正确或等价答案 | 得分 | 错在哪里 |
|---|---|---|---:|---|
| F01 | `int` | `int` | 2/2 | 正确。 |
| F02 | `0:4` | `:4` 或 `0:4` | 2/2 | 等价写法，正确。 |
| F03 | `0:0:-1` | `::-1` | 0/2 | 起止位置都是 0，实际得到空字符串，不能反转整串。 |
| F04 | `strip` | `strip` | 2/2 | 正确。 |
| F05 | `join` | `join` | 2/2 | 正确。 |
| F06 | `*rest` | `rest` | 0/2 | 接收时用 `*rest`，变量名是 `rest`；`print(*rest)` 输出 `b c`，不是列表。 |
| F07 | `numbers%2==0` | `number % 2 == 0` | 0/2 | `numbers` 是整个列表，`number` 才是当前整数；列表不能这样取余。偶数条件的思路是对的，但填入后无法执行。 |
| F08 | `find` | `get` | 0/2 | 字典查询默认值用 `get`；`find` 是字符串查找方法。 |
| F09 | `enmerates` | `items` | 0/2 | 字典键值对遍历用 `items()`；即使改成 `enumerate`，也不是此处要填的字典方法。 |
| F10 | `set` | `set` | 2/2 | 正确。 |

### 判断：6/10

| 题号 | 你的答案 | 正确答案 | 得分 | 说明 |
|---|---|---|---:|---|
| J01 | 对 | 错 | 0/1 | `(7)` 是整数，但整数不能用 `len()`；`(7,)` 才是单元素元组。 |
| J02 | 错 | 错 | 1/1 | 正确，未接收 `upper()` 的结果，原变量仍是 `cat`。 |
| J03 | 对 | 错 | 0/1 | `sort()` 修改原列表，但返回 `None`，所以 `result` 不是排序后的列表。 |
| J04 | 错 | 对 | 0/1 | 元组里的列表可以修改内容；这里没有替换元组的某个位置。 |
| J05 | 错 | 错 | 1/1 | 正确。 |
| J06 | 对 | 对 | 1/1 | 正确。 |
| J07 | 对 | 对 | 1/1 | 正确。 |
| J08 | 对 | 错 | 0/1 | `second = first` 没有复制列表，两个变量指向同一个列表，都会看到变化。 |
| J09 | 错 | 错 | 1/1 | 正确。 |
| J10 | 对 | 对 | 1/1 | 正确。 |

## 三、编程题逐题批改

### D1：代码找错与修复，4/15

| 题号 | 得分 | 按原评分项拆分 | 分析 |
|---|---:|---|---|
| D1.1 | 3/5 | 起点说明0＋右边界说明0＋正确切片2＋保留输入并输出1 | `code[4::]` 正确，能输出 `2026`；问题说明留空，扣2分。 |
| D1.2 | 1/5 | 返回值说明0＋原错误说明0＋使用排序结果输出0＋保留原列表1 | `le = sorted(letters)` 接住了结果，但输出仍拼接 `letters`，实际是 `cab`。原列表顺序保留，得1分。 |
| D1.3 | 0/5 | 原因说明0＋新元组0＋正确更新坐标0＋正确输出0 | 原错误 `point[0] = 6` 未修复，会报 `TypeError`；问题说明未填。 |

D1.2 只需把结果真正接到后续输出：

```python
letters = ["c", "a", "b"]
le = sorted(letters)
print("".join(le))
```

D1.3 要创建新元组，同时保留原来的第二个坐标：

```python
point = (3, 8)
point = (6, point[1])
print(point)
```

### D2：成绩记录处理，4/15

| 题号 | 得分 | 按原评分项拆分 | 分析 |
|---|---:|---|---|
| D2.1 | 2/3 | 循环拆包1＋全部且顺序正确1＋格式0 | 姓名和分数拆得正确，顺序正确；题目要求中文冒号 `：`，作答为英文 `:`。 |
| D2.2 | 1/3 | 正确累计1＋平均分0＋两项规范输出0 | 总分变量可累计到370；`len(sum)` 对整数取长度会报错，整个多行字符串也来不及打印。应除以人数 `len(rows)`，并用 `:.1f` 指定一位小数。 |
| D2.3 | 1/3 | 筛选条件1＋姓名列表0＋输出及空名单处理0 | `p >= 60` 正确；列表没有 `.add()`，应使用 `.append()`；没有名单输出及空名单处理。 |
| D2.4 | 0/3 | 最大值比较0＋姓名分数对应0＋输出0 | `for i in rows` 中的 `i` 是一条元组记录，不是下标；`rows[i]` 会报错，也没有正确的最高分比较、姓名保存及输出。 |
| D2.5 | 0/3 | 分档边界0＋正确计数0＋规定输出0 | 初始化有语法错误；`&` 不是逻辑“并且”；`600` 和 `<70` 也不符合60—79的及格档；整段无法执行，输出还缺“人”。 |

D2.5 已写出了四档计数和按顺序输出的框架，这是有用的尝试；但当前三个评分项都未完整满足，不凭意图补分。

需要分清的变量：

| 名字或表达式 | 实际是什么 | 应怎么用 |
|---|---|---|
| `rows` | 包含5条记录的列表 | `len(rows)` 求人数 |
| `for i in rows` 中的 `i` | 例如 `("林晓", 72)` 的元组 | 拆成姓名和分数，或用 `i[1]` 取分数 |
| `for name, score in rows` | 当前姓名与当前分数 | 用 `score` 判断、累加和比较 |
| 总分变量 | 整数，如370 | 除以人数，不能对它用 `len()` |
| `passed_names` | 姓名列表 | 用 `append(name)` 添加姓名 |

`sum`、`max` 可以作为变量名，但会遮住 Python 的同名内置函数，建议改成 `total_score`、`best_score`。**本次不单独为命名扣分。**

D2.5 的关键修正形式如下；完整成绩处理代码见[参考答案](参考答案与评分.md)的D2部分。

```python
y = l = j = d = 0
for name, score in rows:
    if score >= 90:
        y += 1
    elif score >= 80:
        l += 1
    elif score >= 60:
        j += 1
    else:
        d += 1
print(f"优秀：{y}人")
print(f"良好：{l}人")
print(f"及格：{j}人")
print(f"待加强：{d}人")
```

这里利用了题目已保证分数在0—100之间。进入后面的 `elif`，意味着前面的更高分条件已经不成立。

### D3：订单数量汇总，1/20

| 题号 | 得分 | 已满足与未满足的评分项 |
|---|---:|---|
| D3.1 | 1/4 | 按分号拆分得1分；没有去两端空白、排除空项、保存有效记录并打印正确条数。 |
| D3.2 | 0/4 | 只初始化 `records = []`，尚未拆字段、清洗名称、转换整数、组成元组列表并打印。 |
| D3.3 | 0/4 | 未作答，未建立及累计字典，也未输出。 |
| D3.4 | 0/4 | 未作答，未用集合去重、统计与排序输出。 |
| D3.5 | 0/4 | 未作答，未计算最大累计量、保留并列商品及排序输出。 |

你在 F04 写对了 `strip`，在 D3.1 却想不起来。这说明至少部分困难是**在真实处理步骤中调用知识不够熟练**，不能全部归结为没学过；但 F08、F09 的错误又说明字典方法确实需要重新分清。

先把 D3 看成六步，不要求一次写完整段：

1. 按分号拆开整段文字。
2. 每段 `strip()`，空的丢掉。
3. 按逗号拆出商品和数量，商品转小写，数量转整数。
4. 用字典把同名商品的数量累加。
5. 用集合保存商品名，再排序输出。
6. 找最大累计数量，再找出所有等于最大数量的商品。

当前先独立完成前两步，即 D3.1；再练 D3.2、D3.3。D3.4—D3.5 是后续目标。完整代码已在[参考答案](参考答案与评分.md)，不必背整段。

## 四、为什么点击运行没有结果

已用本机 **Python 3.12.6** 检查原答卷，并把各小问分开验证。可确认的首个阻塞在原答卷 **第128行**：

```python
y=0,l=0,j=0,d=0
```

这是 `SyntaxError`（语法错误）。运行 `.py` 文件前，Python 会先检查整份文件的语法；即使这一行在后面，前面的 `print` 也不会执行。终端应能看到错误提示，**没有正常结果不等于没有报错信息**。这能解释当前文件的表现，但没有直接检查你的编辑器界面。

把这一行修好后，也不会自动全卷运行成功：从头运行会先到 D1.3 的元组赋值错误；单独运行 D2.2、D2.3、D2.4，又分别存在整数取长度、列表调用 `add`、元组当列表下标的问题。

建议订正时这样做：

1. 保留原答卷，另存一份订正文件，或使用临时练习文件。
2. 一次只运行一个小问，并带上该题的初始数据。不要把后面仍有语法错误的题一起运行。
3. 看终端报错的最后一行，定位错误类型，再看指向哪一行。
4. 修改一个问题后重新运行，核对实际输出与题目要求。
5. 当前小问通过后，再增加下一问；最后才检查整段是否能连起来运行。

单独运行 D1.1，原作答确实输出 `2026`；D1.2 输出 `cab`。这些结果已用于逐问评分，没有因第128行的错误重复扣掉整卷分数。

## 五、综合评价与下一步

**当前处在“Python基础与容器已接触，需要巩固独立编程”的阶段。** 课程观看进度沿用原记录的 P62，本次没有新的观看进度证据。

已表现出的能力：多数选择题正确；能识别 `range`、列表基本变化、字符串拆分、单元素元组和字典更新；能写循环拆包，能写出总分累加与及格判断。没有必要从第一节全部重学。

优先补的能力：说清每个变量是什么类型；区分修改原对象与返回新结果；正确选择列表、字符串、字典的方法；把中间结果接到后续计算；读报错并逐段验证。问题不只是“单词拼写忘了”，也包含对象和处理步骤没有分清。

本卷不考函数，因此**不能凭这张卷子判定函数已经掌握或完全不会**。建议先用接下来的2—3次学习补基础，实际未通过就延长；P63以后的推进随补练结果调整，不按旧日期赶课。

| 顺序 | 建议任务 | 完成标志 |
|---|---|---|
| 下次第1步 | 20—30分钟：分题运行 D1.1—D1.3，读懂语法错误与运行时错误；每题补一句原因 | 能说明为什么整卷没有正常输出，能分别得到2026、abc、(6, 8) |
| 接着做 | 30—45分钟：订正 D2.1—D2.3 | 能得到370、74.0及正确名单；换成全不及格数据时输出“无” |
| 下一次 | 45—60分钟：D2.4—D2.5，再单独练 F08、F09 | 最高分姓名与分数对应；59、60、79、80、89、90分档正确；能说明 `get` 和 `items` 的用途 |
| 随后 | 30—45分钟：先 D3.1，再 D3.2—D3.3 | 原数据得到5条有效记录，商品统一小写，累计量为eraser 1、notebook 6、pen 6 |
| 隔天检查 | 合上答案换数据，重做已订正的基础小问 | 能解释并独立完成，再恢复函数基础 F01—F04；达标后继续函数进阶 |

看懂本报告、照抄修正代码，都只算订正过程。换数据独立完成后，才有新的掌握证据。无需立刻再做一张完整试卷。

**本报告中的失分分析不自动收入04。由你选择值得保留的题，再明确要求补充错题或疑问。** 持续阶段判断与下一步建议见[07 学习阶段与下一步](../../07_学习阶段与下一步/README.md)。

[返回试卷入口](README.md) · [返回总导航](../../README.md)
