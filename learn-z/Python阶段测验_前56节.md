# Python 阶段巩固卷 · 前 56 节

**满分：100 分｜建议用时：120 分钟｜基础：80 分｜综合拔高：20 分**

姓名：________　日期：________　实际用时：________　得分：________

## 考查范围与作答规则

范围依据：[黑马程序员 Python + AI 课程](https://www.bilibili.com/video/BV1sHU9BmEne/?p=56)前 56 节的目录。第 56 节为“数据容器总结”，第 57 节开始函数基础。范围已于 2026-09-15 核对。

本卷考查变量、类型转换、输入输出、运算符、判断、循环，以及列表、字符串、元组、集合、字典。使用 Python 3.12；不要求自定义函数、类、文件读写、第三方库或 `lambda`。

1. 在 [Python阶段测验_答题.py](Python阶段测验_答题.py) 中作答。选择、填空、判断先独立推理，不运行题目代码；编程大题允许运行和调试。
2. 先完成整卷，再打开 [Python阶段测验_答案与评分.md](Python阶段测验_答案与评分.md)。不会的题可以标记后跳过，不需要卡在一道题上。
3. 每道选择题只有一个正确选项；填空每题只有一空；判断题填写“对”或“错”，不要求写理由。
4. 不同题的代码相互独立。D1 的三个小问也相互独立；D2、D3 各自内部的小问可以衔接，但不依赖其他大题的变量。
5. 大题不能直接写死答案，必须根据给定数据处理。除题目明确要求的方法外，普通循环和正确的等价写法同样得分。
6. 输出内容、顺序、小数位数按题目要求；不考中文在终端中占几格。不得通过修改题目数据避开难点。

| 部分 | 题号 | 分值 | 建议用时 |
|---|---|---:|---:|
| 单项选择 | S01—S10 | 10 × 2 = 20 分 | 20 分钟 |
| 填空 | F01—F10 | 10 × 2 = 20 分 | 20 分钟 |
| 判断 | J01—J10 | 10 × 1 = 10 分 | 10 分钟 |
| 大题一：找错与修复 | D1.1—D1.3 | 3 × 5 = 15 分 | 15 分钟 |
| 大题二：成绩记录处理 | D2.1—D2.5 | 5 × 3 = 15 分 | 20 分钟 |
| 大题三：订单数量汇总 | D3.1—D3.5 | 5 × 4 = 20 分 | 25 分钟 |
| 检查漏题、输出与题号 | — | — | 10 分钟 |
| **合计** | | **100 分** | **120 分钟** |

---

## 一、单项选择题（共 20 分）

### S01 · 输入的类型（2 分）

运行下面的代码，在键盘上输入 `18` 并回车。不计输入过程，最后一行输出什么？

```python
age = input("请输入年龄：")
print(age + "2")
```

- A. `20`
- B. `182`
- C. `18 2`
- D. 抛出 `TypeError`

### S02 · 算术运算（2 分）

```python
print(17 // 5, 17 % 5, 2 ** 3)
```

- A. `3.4 2 6`
- B. `3 3 8`
- C. `3 2 8`
- D. `3 2 6`

### S03 · 条件执行顺序（2 分）

```python
score = 75
if score >= 60:
    print("及格")
elif score >= 70:
    print("良好")
else:
    print("待加强")
```

- A. 只输出 `及格`
- B. 只输出 `良好`
- C. 先输出 `及格`，再输出 `良好`
- D. 只输出 `待加强`

### S04 · continue 与 break（2 分）

```python
number = 0
while number < 5:
    number += 1
    if number == 2:
        continue
    if number == 4:
        break
    print(number)
```

下面用逗号表示换行，实际输出的数字依次是：

- A. `1, 2, 3`
- B. `1, 3, 4`
- C. `0, 1, 3`
- D. `1, 3`

### S05 · range 的边界与步长（2 分）

```python
numbers = []
for number in range(2, 7, 2):
    numbers.append(number)
print(numbers)
```

- A. `[2, 4, 6, 7]`
- B. `[2, 4, 6]`
- C. `[2, 3, 4, 5, 6]`
- D. `[0, 2, 4, 6]`

### S06 · 列表连续变化（2 分）

```python
numbers = [4, 1]
numbers.append(3)
numbers[0] = 2
numbers.pop(1)
print(numbers)
```

- A. `[4, 3]`
- B. `[2, 1]`
- C. `[2, 3]`
- D. `[2, 1, 3]`

### S07 · 连续分隔符（2 分）

```python
print("a,b,,c".split(","))
```

- A. `['a', 'b', '', 'c']`
- B. `['a', 'b', 'c']`
- C. `['a,b,,c']`
- D. `['a', 'b', ',', 'c']`

### S08 · 单元素元组（2 分）

```python
item = ("book",)
```

`item` 的类型和 `len(item)` 分别是：

- A. `str`，`4`
- B. `tuple`，`4`
- C. `str`，`1`
- D. `tuple`，`1`

### S09 · 选择容器（2 分）

要保存学生的签到记录，需要**保留录入顺序、允许重复姓名，还能通过下标修改某一条记录**。下面哪种类型最合适？

- A. 字符串 `str`
- B. 列表 `list`
- C. 元组 `tuple`
- D. 集合 `set`

### S10 · 字典更新（2 分）

```python
stock = {"pen": 3, "book": 2}
stock["pen"] = stock["pen"] + 4
stock["eraser"] = 1
print(len(stock), stock["pen"])
```

- A. `4 7`
- B. `3 4`
- C. `3 7`
- D. `2 7`

---

## 二、填空题（共 20 分）

每题一空，每空 2 分。只填写 `____` 处缺少的内容，不重复空白外已有的括号、引号等符号。语义等价的正确写法均可。

### F01 · 类型转换（2 分）

把字符串转为整数，使输出为 `30`。

```python
print(____("24") + 6)
```

### F02 · 取前四个字符（2 分）

```python
text = "Data2026"
print(text[____])  # 输出 Data
```

### F03 · 反转字符串（2 分）

```python
word = "robot"
print(word[____])  # 输出 tobor
```

### F04 · 清洗两端空白（2 分）

```python
raw = " \n PYTHON \t"
clean = raw.____().lower()
print(clean)  # 输出 python，两端没有空格、换行或制表符
```

### F05 · 拼接（2 分）

```python
colors = ["red", "blue"]
print(" / ".____(colors))  # 输出 red / blue
```

### F06 · 星号拆包（2 分）

```python
first, *rest = ("a", "b", "c")
print(____)  # 输出 ['b', 'c']，填写接住剩余元素的变量名
```

### F07 · 筛选条件（2 分）

填入“`number` 是偶数”的条件。该条件应适用于其他整数，不得只列举本例中的三个偶数。

```python
numbers = [1, 2, 3, 4, 5, 6]
even_numbers = [number for number in numbers if ____]
print(even_numbers)  # 输出 [2, 4, 6]
```

### F08 · 字典查询（2 分）

查不到指定键时返回默认值 `0`，不报错。

```python
counts = {"pen": 2}
print(counts.____("book", 0))  # 输出 0
```

### F09 · 遍历字典的键和值（2 分）

```python
scores = {"Lin": 81, "Qin": 92}
for name, score in scores.____():
    print(name, score)
```

### F10 · 集合去重（2 分）

将 `items` 转成集合，再统计不同数字的个数。

```python
items = [3, 1, 3, 2]
unique_items = ____(items)
print(len(unique_items))  # 输出 3
```

---

## 三、判断题（共 10 分）

每题 1 分，填写“对”或“错”。

### J01 · 整数与长度（1 分）

`value = (7)` 创建了一个整数，因此 `len(value)` 的结果是 `1`。

### J02 · 字符串方法（1 分）

下面代码最后输出 `CAT`。

```python
text = "cat"
text.upper()
print(text)
```

### J03 · sort 的返回值（1 分）

下面代码运行后，`numbers` 和 `result` 的值都是 `[1, 2, 3]`。

```python
numbers = [3, 1, 2]
result = numbers.sort()
```

### J04 · 元组中的列表（1 分）

下面代码可以成功执行，最后 `data` 为 `(1, [2, 3, 4])`。

```python
data = (1, [2, 3])
data[1].append(4)
```

### J05 · 集合的下标（1 分）

`letters = {"a", "b"}` 可以通过 `letters[0]` 取出第一个元素。

### J06 · 字符串成员判断（1 分）

`"ab" in "zabc"` 的结果是 `True`。

### J07 · range 的长度（1 分）

`len(range(1, 5))` 的结果是 `4`。

### J08 · 列表赋值（1 分）

下面代码运行后，`first` 仍为 `[1, 2]`，只有 `second` 变为 `[1, 2, 3]`。

```python
first = [1, 2]
second = first
second.append(3)
```

### J09 · 空容器（1 分）

`{}` 和 `set()` 创建的都是空字典。

### J10 · match 分支（1 分）

在 Python 3.12 中，下面代码只输出一行 `启动`。

```python
command = "start"
match command:
    case "start":
        print("启动")
    case _:
        print("未知命令")
```

---

## 四、大题一：代码找错与修复（D1，共 15 分）

每问 5 分：**指出问题 2 分，修复正确 3 分**。三个小问分别作答，每问都要写一句问题说明和修复后的代码。

### D1.1 · 切片范围（5 分）

目标是从 `code` 中取出年份，输出 `2026`。下面的代码没有达到目标，请修复切片表达式，不能修改 `code` 的内容。

```python
code = "CODE2026"
print(code[3:7])
```

### D1.2 · 排序结果去了哪里（5 分）

目标是输出 `abc`，同时保留 `letters` 原有的顺序 `['c', 'a', 'b']`。下面的代码没有达到目标，请修复。

```python
letters = ["c", "a", "b"]
sorted(letters)
print("".join(letters))
```

### D1.3 · 更新坐标（5 分）

目标是把 `point` 更新为 `(6, 8)`，并保持它是元组。下面的代码会报错，请说明报错原因并修复。

要求：第二个坐标从原来的 `point` 中保留，不能重新写死为 `8`。

```python
point = (3, 8)
point[0] = 6
print(point)
```

---

## 五、大题二：成绩记录处理（D2，共 15 分）

每问 3 分。只使用本题给定的数据，完成下面五项要求。

```python
rows = [("林晓", 72), ("周宁", 59), ("陈雨", 91), ("苏禾", 88), ("许安", 60)]
```

约定：名单非空，姓名不重复，分数为 `0`—`100` 的整数，最高分唯一。不要修改 `rows`，结果必须由代码计算得到。

### D2.1 · 逐条打印（3 分）

用循环拆出姓名和分数，按原顺序输出：

```text
林晓：72分
周宁：59分
陈雨：91分
苏禾：88分
许安：60分
```

### D2.2 · 总分与平均分（3 分）

计算总分、平均分；平均分保留一位小数。输出：

```text
总分：370
平均分：74.0
```

### D2.3 · 及格名单（3 分）

筛选分数**大于等于 60** 的学生，把姓名保存到列表 `passed_names`，顺序与原名单一致，再输出：

```text
及格名单：林晓、陈雨、苏禾、许安
```

如果更换数据后没有人及格，则输出 `及格名单：无`。

### D2.4 · 最高分（3 分）

找出分数最高的学生，输出：

```text
最高分：陈雨 91分
```

### D2.5 · 分数分档（3 分）

按下面四个**互不重叠**的区间统计人数：

- 优秀：`90`—`100`。
- 良好：`80`—`89`。
- 及格：`60`—`79`。
- 待加强：`0`—`59`。

按上述顺序输出：

```text
优秀：1人
良好：1人
及格：2人
待加强：1人
```

注意：D2.3 的“及格名单”包含所有 `>= 60` 的人；D2.5 的“及格档”只包含 `60`—`79` 分，两个统计范围不同。

---

## 六、大题三：订单数量汇总（D3，共 20 分，综合拔高）

每问 4 分。本题把学过的几种容器连起来使用，可以拆成多步完成，不要求写成一行。

### 原始数据与规则

```python
raw_orders = " Pen , 2 ; notebook,3;; PEN,4; eraser , 1; notebook ,3;   ;\n"
```

数据约定：

- 分号 `;` 分隔记录，逗号 `,` 分隔商品名和数量。
- 连续分号、只含空白的片段、末尾空白不算订单，必须忽略。
- 有效记录中恰好有一个逗号；商品名非空，只含英文字母，允许大小写不同及字段两端空白。
- 商品名统一为小写后再统计，`Pen` 和 `PEN` 是同一种商品。
- 数量转换为整数；有效数量保证为正整数，不考非法数字和异常处理。
- 至少有一条有效记录；同名商品可以出现多次，销量最高的商品可以并列。
- 所有商品清单均按**统一小写后的商品名字母升序**输出；不直接依赖集合的显示顺序。

### D3.1 · 拆分记录并去掉空项（4 分）

把有效记录保存到列表 `clean_rows` 中：去掉每段记录两端空白，保留非空记录，顺序与输入一致。此时不用改记录内部的空格。

输出：

```text
有效记录：5条
```

### D3.2 · 整理成元组记录（4 分）

进一步清洗每条记录，创建列表 `records`，每个元素都是 `(小写商品名, 整数数量)`，顺序与有效记录一致。打印 `records`，结果应为：

```text
[('pen', 2), ('notebook', 3), ('pen', 4), ('eraser', 1), ('notebook', 3)]
```

### D3.3 · 按商品累计数量（4 分）

创建字典 `totals`，键是商品名，值是这种商品的累计数量。按商品名字母升序逐行输出：

```text
eraser：1件
notebook：6件
pen：6件
```

### D3.4 · 商品去重与种类统计（4 分）

用集合 `product_names` 保存不重复的商品名，统计种类数，并按商品名字母升序输出清单：

```text
商品种类：3种
商品清单：eraser、notebook、pen
```

### D3.5 · 找出全部最高销量商品（4 分）

找出**累计数量**最多的商品。如果并列，要全部保留并按商品名字母升序输出：

```text
最高销量：6件
最高销量商品：notebook、pen
```

更换数据后，如果最高销量商品只有一个，也按照同样格式输出该商品名。

---

## 交卷前检查

- [ ] S01—S10、F01—F10、J01—J10 都已作答或标记不会。
- [ ] D1 每问都有问题说明和修复代码。
- [ ] D2、D3 每个小问都检查了要求的变量、数据类型与输出。
- [ ] 没有修改输入数据，也没有把预期结果直接写成答案。
- [ ] 需要的计算结果已经保存、打印或用于后续处理。
- [ ] 已记录用时，并保存答题文件。

完成后再打开独立答案。分数用于定位下一步复习内容，不代表全部 Python 能力。
